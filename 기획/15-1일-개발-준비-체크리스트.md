# 15. 1일 개발 준비 체크리스트

## 1. 이미 준비된 것 (daily-scrum-sprinton 기준)

- 기획·UI·인증·ERD·행동·에러 메시지·컬럼 매핑(00~14).
- 배치 시점·Member/인증·report.type·API 후보·두레이 조회·AI 규격 정리 완료.

## 2. 추가로 준비할 것

- **실행 가능한 DDL**: MySQL `CREATE TABLE` 한 파일 (06 기준으로 작성).
- **REST API 스펙 확정**: 11 경로 후보 → 각 API별 **요청/응답 예시(JSON)** 고정.
- **1일 MVP 범위**: 당일 구현할 API/화면 목록 한 줄로 고정.

## 3. 1일차 진행

- DDL 적용 → BE API 구현 → FE 화면·연동. Claude Code에 DDL·API 스펙·06 컬럼명 주고 Controller/Service/Mapper/DTO 생성 요청.

## 결론

미리 서류·API·ERD·DDL을 정리해 두면 **1일 개발만으로 현실 가능**. 스펙 고정 시 당일 설계 논의 없이 구현만 진행 가능.
